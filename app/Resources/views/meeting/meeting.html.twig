{% extends 'base.html.twig' %}

{% block pageHeader %} {{ pageHeader }}{% endblock %}
{% block subHeader %} {{ subHeader }} {% endblock %}

{% block body %}

    <script type="text/javascript">
        $(document).ready(function () {
            $("#myTab li:eq(1) a").tab('show');
        });
    </script>

    <ul class="nav nav-tabs" id="myTab">
        <li class="active"><a data-toggle="tab" href="#attendance">User Attendance </a></li>
        <li><a data-toggle="tab" href="#agendas">Agendas</a></li>
    </ul>
    <div class="tab-content">
        <div id="attendance" class="tab-pane fade in active">

            <div class="col-xs-6">
                {{ form_start(form) }}
                {#{{ form_widget(form.attendance) }}#}
                {{ form_row(form.attendance) }}
                {{ form_row(form.apologiesNote ,{'value' : currNote }) }}

                <button type="submit" class="btn btn-primary" formnovalidate><i class="fa fa-arrow-right"
                                                                                aria-hidden="true"></i>Update Attendance
                </button>
                {{ form_end(form) }}
            </div>


            <table class="table table-striped">
                <caption>Meetings participants attendance list</caption>
                <thead>
                <tr>
                    <th>User</th>
                    <th>Declared presence</th>
                    <th>Apologies note</th>
                </tr>
                </thead>
                <tbody>
                {% for userAttendance in usersAttendanceList %}
                    <tr>

                        <td>{{ userAttendance.user.fullName }}</td>
                        <td>{{ userAttendance.attendance }}</td>
                        <td>{{ userAttendance.apologiesNote }}</td>

                    </tr>
                {% endfor %}
                </tbody>
            </table>

        </div>
        <div id="agendas" class="tab-pane fade">
            <h3>Section B</h3>
            <p>Vestibulum nec erat eu nulla rhoncus fringilla ut non neque. Vivamus nibh urna, ornare id gravida ut,
                mollis a magna. Aliquam porttitor condimentum nisi, eu viverra ipsum porta ut. Nam hendrerit bibendum
                turpis, sed molestie mi fermentum id. Aenean volutpat velit sem. Sed consequat ante in rutrum convallis.
                Nunc facilisis leo at faucibus adipiscing.</p>
        </div>
    </div>
{% endblock %}


{% extends 'base.html.twig' %}
{% block pageHeader %}
    {{ pageHeader }}
{% endblock %}
{% block subHeader %}
    {{ subHeader }}
{% endblock %}

{% block body %}
    <h3> User to delete</h3>
    <p>{{ userToDelete.fullName }} ({{ userToDelete.email }})</p>

        {{ form_start(form) }}
        <button type="submit" class="btn btn-primary" formnovalidate>Delete</button>
        {{ form_end(form) }}
{% endblock %}

